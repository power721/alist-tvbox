# AList-TvBox

TVBox VOD 后端 + 网盘聚合（PowerList/AList）+ 电报搜索 + 直播聚合。本文件是**术语表**，只定义本项目特有的概念，不含实现细节。

## 追更（Media Subscription）

**订阅 / Subscription**：
用户对一部作品的追更关系。当前一个订阅对应一季（`season` 字段），持有固定挂载路径、筛选条件与追更状态。
_Avoid_: 追剧记录、Media（本项目不存在独立的 Media 实体，订阅即媒体）

**剧集 / Episode**：
作品的一集，由 `(订阅, 季号, 集号)` 唯一确定。**特别篇的季号为 0，电影为 S1E1**（对齐 TMDB/Emby/Jellyfin 约定）。剧集**不持有可用性**——可用性由集源聚合派生。
_Avoid_: 分集、集数（"集数"指数量，"剧集"指实体）

**资源 / Resource**：
一个网盘分享链接及其在本订阅下的候选记录。资源的状态只表达**生命周期位置**（候选 / 已挂载 / 已卸载 / 已拒绝），**不表达可用性**。
_Avoid_: 分享、Share（`Share` 是 AList 挂载实体，与资源是不同层次）、片源

**集源 / EpisodeSource**：
「某一集在某个资源里的那个文件」。剧集与资源的多对多连接，持有分享内相对路径与该文件的真实可用状态。**这是全系统唯一记录可用性的地方。**
_Avoid_: 播放源（"播放源"在需求语境里含混，既指分享也指集文件）

**季号 / Season**：
订阅所追的季。它同时是**匹配关卡**（拒绝标注了其它季的候选）和**解析参数**（`SxxEyy` 文件名的季必须与之相符才算数）——两处都会因季号错误而静默丢弃全部资源，所以它必须与剧名一致，不能各入口自行取默认值。

**集号解析 / Episode Parsing**：
从网盘目录里的文件名（与其所在目录名）判定这是第几季第几集。信号强度分明：**目录名声明的季 > 文件名 `SxxEyy` > 文件名裸数字**。裸数字只是兜底，`[2026.08.21]` 这类日期戳必须先剥掉——**文件名里的数字 ≠ 集号**。同理，多季合集目录里的「第01集」属于它所在的那一季，不属于订阅追的那一季。
_Avoid_: 集数识别（"集数"指数量）

**候选池 / Candidate Pool**：
一个订阅下尚未挂载但已入库的资源集合。存在目的是让失效恢复**不需要现场搜索**。

**挂载路径 / Mount Path**：
订阅在 AList 下的固定路径 `/追剧/{id}-{名称}`。换源只替换该路径背后 storage 的配置，**路径本身永不改变**——这是播放地址与观看进度不断链的地基。

**逻辑链接 / msubep**：
TVBox 播放条目 `msubep-{订阅}-{集}`。它不是地址，是**待解析的意图**：播放时才实时选源。副作用是它把真实集号带进了播放历史，因而也是观看进度的识别依据。

**主网盘 / Main Drive**：
用户指定要维持完整覆盖的网盘类型（全局 Setting 或订阅级覆盖，取前 2）。主网盘候选在打分和入池配额上都有保底。

**失效黑名单 / dead link**：
判定为失效的分享链接的全局记录。**跨订阅共享的唯一内容**——一个分享死了，所有订阅立刻都知道。

**巡检 / Check**：
对单个订阅的定期检查（刷元数据 → 重列挂载 → 缺集判定 → 补搜/换源）。按 `next_check_time` 排队，队列是查询而非表。

**探测 / Probe**：
把候选资源实际挂载并列出集数，得到它到底覆盖哪些集。**探测产出事实**，与"从标题猜测覆盖"相对。

**取链 / Link Resolution**：
向网盘请求某个文件的实际播放地址。**"能列出目录" ≠ "能取到链"** —— 分享有效但文件被和谐时前者成功、后者失败。取链结果是系统内**唯一可信的可用性信号**。

**缺集 / Gap**：
官方已播（或期望）集数与本地已有集数之差。缺集触发补搜。
_Avoid_: 补缺源（该概念已废弃：资源不再分主源/补缺源，统一按集源排序选取）

**最大集号 / max episode**：
挂载目录里观测到的最大集号，属于**资源侧**指标。
_Avoid_: 最后观看集数（那是观看进度，来自 `History` 表，两者曾被同一字段混淆）

**观看进度 / Watch Progress**：
用户对某订阅看到第几集，读自 `History`（`uid` + `vodId`），追更系统不自行存储。多端进度由播放记录同步天然合并。

**追更模式 / FOLLOW**：
分享固定挂载、失效自动换源、缺集多源合并。无需网盘账号。

**转存模式 / TRANSFER**：
增量把缺失文件转存到用户自己的网盘，之后优先播放自有副本。转存永远是增量的——不存在"整包转存"。

## 多用户（Multi-User）

**凭证不下发给非归属人 / Credentials for owners only**：
网盘账号的 cookie/token/密码只随直链下发给账号归属人（本人的 TvBox 拿自己账号的 Cookie 是可接受的）。直连允许携带的 header 仅限公开 UA/Referer；非归属人播放一律走服务端代理地址。共享 token 视为管理员设备，可拿全部凭证。

**全局账号 / Global Account**：
管理员添加、`ownerUid=0` 的网盘账号。`shared=true` 时普通用户可以**经服务端代理**使用（播放/列目录），但凭证对普通用户不可见、不下发。仅管理员订阅可将其作为转存目标。

**我的账号 / Personal Account**：
普通用户自己添加的网盘账号（`ownerUid=该用户`）。全功能归本人使用（播放直连+Cookie、转存、盘搜），管理接口按归属人隔离。用户删除时级联销毁。

_Avoid_: 共享账号（「共享」指 shared 标记的行为，账号实体只有「全局/个人」两种归属；说「共享账号」时先想清楚指的是全局账号开了 shared，还是 AList 的 Share 挂载）
