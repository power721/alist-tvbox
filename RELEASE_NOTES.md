# Release Notes - 1.66.0

## 新增

- 追剧候选资源列表显示单集平均文件大小,挑选网盘资源时可直接比较体积,不用再逐个点开看

## 修复

- 修复每次重启后豆瓣增量数据被抹掉的问题(「刚才还有元数据,重启后没有了」):小雅基线 data.sql 是全量还原脚本,每次启动都被重放,把影片元数据打回基线,增量行全灭;现在检测到基线已载入时会自动中和该脚本,并重放全部增量恢复被抹掉的数据
- 修复基线重灌后全量重放从不触发的问题:重灌标记路径由 /tmp/base_version 修正为 /data/atv/base_version,重新灌注基线后会正确清空增量记录并触发全量重放
