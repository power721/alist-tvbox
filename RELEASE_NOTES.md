# Release Notes - 1.72.1

## 新增

- gui/web 端详情与播放列表接口下发网盘外挂字幕,网页端播放同样可选外挂字幕轨

## 修复

- 猫源自定义爬虫兼容性再修复:支持「源码复制」形态的爬虫(引用源码树相对路径的依赖自动映射,saohuo/tiantian 等实测装载成功)、中文文件名(如「次元成(猫源).js」)不再被安全检查误拒;心跳诊断信标合法化,消除服务端 404 与错误日志噪声

- 修复网盘外挂 ass 字幕「能选中但不出字」的问题:外链 ass 字幕的格式声明被误标为 srt,播放器按 srt 解析 ASS 内容静默失败;现与 ssa 统一标记为 text/x-ssa,选中即可正常显示(含特效字幕)
- 修复猫源自定义爬虫在原版猫影视、UZN、PeekPro 三类客户端的兼容问题:补全 basic auth 质询握手(原版猫影视需要 401 挑战才会带上凭证,直接返回数据会被判为空)、启动瞬间即装载磁盘缓存(uzn 系只在启动一瞬拉取配置)、网络层与路由分发全面加固,三端站点数据均可正常加载
