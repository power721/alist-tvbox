# Release Notes - 1.83.0

## 新增

- 播放列表支持按音轨语言拆分线路:同一目录中国语/粤语文件混排时自动分组为独立播放线路,单语言目录保持单线路不变
- 115 索引新增「强制更新」:配置页新增强制更新按钮,可无条件重新下载;重装后版本号残留、数据目录被清空等场景会在定时任务中自动重下自愈

## 优化

- B站详情改为三请求并发加载,进入详情更快;斗鱼直播取流提速,并恢复每条线路的全部清晰度选项
- 资源列表页记住上次的排序与网盘类型筛选:翻页、修改每页条数后,排序/筛选/搜索词不再被重置
- 饭太硬 MDrive 爬虫预置本地凭证,支持使用服务端下发的夸克/UC cookie,减少扫码(需更新爬虫插件后生效)
- 播放、直播、巡检、搜索、账号等多处稳定性与安全加固

## 修复

- 修复全新订阅手动添加网盘目录后,App 端始终提示「尚未找到可用资源」、选集为空的问题(#1071)
- 修复纯净版部署混入小雅布局的豆瓣挂载路径,导致搜索、分类浏览返回不可播放「幽灵路径」的问题:纯净版不再写入此类数据,已被污染的数据库启动时自动清理
- 修复一键脚本升级、重建或卸载容器时丢失网页上传文件(如 py 过滤插件)的问题:检测到 /www/static 未挂载时先自动抢救到宿主机再删除容器
- 修复标题含「|」等特殊字符时,TG 系搜索与详情请求被拦截返回 400、搜不出结果的问题(需更新爬虫插件后生效)
- 提升百度分享死链识别:分享页 404 等确认失效的链接立即判死,不再反复无效重试
- 修复 TvBox 目录播放的接线问题
