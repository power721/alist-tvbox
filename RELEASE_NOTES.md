# Release Notes - 1.69.0

## 新增

- 追剧支持手动添加候选资源:粘贴分享链接即可入资源池参与补缺,不会改动主源;手动添加的资源不受网盘白名单/年份/标题匹配限制,重复添加同一链接自动恢复

## 优化

- 追剧候选源的「启用」改为仅挂载补缺位并触发一轮巡检,不再自动替换主源;更换主源请使用「转主源/钉选」,挂载失败会留在资源池继续探测
- 完结剧巡检调整到凌晨时段(默认 03:15)自动检查,避开晚高峰;在播剧仍按原高峰档位不受影响
- 新建订阅的单集上/下限默认改为「跟随全局」(0),不再固化硬编码值;存量订阅把数值改为 0 即可恢复跟随全局

## 修复

- 修复爱奇艺排播时刻获取失败、剧集更新时间滞留默认 20:00 的问题(桌面 UA 被重定向到 www 空壳页,m 站改用手机 UA)
- 修复豆瓣电影数据增量更新卡死/失败的问题:json 格式更新改原生写入不再因行缺失异常中断,失败按类型分类重试(确定性错误不反复重试),基线升级后增量链自动重新拉取补齐
