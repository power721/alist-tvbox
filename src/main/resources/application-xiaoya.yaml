spring:
  datasource:
    url: jdbc:h2:file:/data/atv
    username: sa
    password: password
    driverClassName: org.h2.Driver
  sql:
    init:
      data-locations: file:/opt/atv/data/data.sql

logging:
  file:
    name: log/app.log

app:
  xiaoya: true
  sites:
    - name: 丫仙女
      url: http://localhost
      version: 3
      searchable: true
      xiaoya: true
