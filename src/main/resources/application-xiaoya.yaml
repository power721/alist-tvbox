spring:
  datasource:
    jdbc-url: jdbc:h2:file:/data/atv;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    username: sa
    password: password
    driverClassName: org.h2.Driver
  sql:
    init:
      data-locations: file:/data/atv/data.sql

logging:
  file:
    name: log/app.log
  level:
    cn.har01d: info
app:
  xiaoya: true
  sites:
    - name: 丫仙女
      url: http://localhost
      version: 4
      searchable: true
      xiaoya: true
